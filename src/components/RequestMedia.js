import React from 'react';

const RequestMedia = () => {
  return (
    <div style={{padding: '0.5rem'}}>
      <div style={{fontWeight: 700}}>Media</div>
      <div style={{marginTop: '0.5rem', color: '#666'}}>Image thumbnails / uploads go here.</div>
    </div>
  );
};

export default RequestMedia;
