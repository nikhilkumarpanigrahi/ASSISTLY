import React from 'react';

const MapView = () => {
  return (
    <div style={{height: 120, background: '#e9f5ff', borderRadius: 8, display: 'flex', alignItems: 'center', justifyContent: 'center'}}>
      <div style={{color: '#1565c0'}}>Map preview (placeholder)</div>
    </div>
  );
};

export default MapView;
