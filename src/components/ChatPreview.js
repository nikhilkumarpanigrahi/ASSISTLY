import React from 'react';

const ChatPreview = () => {
  return (
    <div style={{padding: '0.5rem'}}>
      <div style={{fontWeight: 700}}>Chat</div>
      <div style={{marginTop: '0.5rem', color: '#666'}}>Recent message preview: "I'll be there at 3pm"</div>
    </div>
  );
};

export default ChatPreview;
