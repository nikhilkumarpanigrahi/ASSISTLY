import React from 'react';

const AdminPanel = () => {
  return (
    <div style={{padding: '0.5rem'}}>
      <div style={{fontWeight: 700}}>Admin Preview</div>
      <div style={{color: '#666', marginTop: '0.5rem'}}>Moderation tools and reports.</div>
    </div>
  );
};

export default AdminPanel;
